# ==============================================================================
# MT3 Worker Dependencies
# ==============================================================================
# 注意: JAX, TensorFlow, MT3 等核心依赖在 Dockerfile 中单独安装
# 这里只包含应用层依赖

# Celery 任务队列
celery==5.3.4
redis==5.0.1

# 音频处理
librosa>=0.10.0
soundfile>=0.12.1
pydub>=0.25.1

# MIDI 处理
pretty_midi>=0.2.10
mido>=1.3.0

# 文件处理
python-multipart==0.0.6

# 工具库
tqdm>=4.66.0
